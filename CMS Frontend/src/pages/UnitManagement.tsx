import { DefaultProps } from "../types";
import { Box, Typography } from "@material-ui/core";

export default function UnitManagement(props: DefaultProps) {

  return (
    <Box id="unitmanagement">
      <Typography>Unit management</Typography>
    </Box>
  )
}