import { DefaultProps } from "../types";
import { Box, Typography } from "@material-ui/core";

export default function MajorManagement(props: DefaultProps) {

  return (
    <Box id="majormanagement">
      <Typography>Major management</Typography>
    </Box>
  )
}