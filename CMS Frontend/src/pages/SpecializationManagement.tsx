import { DefaultProps } from "../types";
import { Box, Typography } from "@material-ui/core";

export default function SpecializationManagement(props: DefaultProps) {

  return (
    <Box id="specializationmanagement">
      <Typography>Specialization management</Typography>
    </Box>
  )
}