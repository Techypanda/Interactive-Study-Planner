import { DefaultProps } from "../types";
import { Box, Typography } from "@material-ui/core";

export default function CareerManagement(props: DefaultProps) {

  return (
    <Box id="careermanagement">
      <Typography>Career Management</Typography>
    </Box>
  )
}