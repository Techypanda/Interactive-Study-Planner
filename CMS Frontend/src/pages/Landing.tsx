import { Box, Typography } from "@material-ui/core";
import { DefaultProps } from "../types";

export default function Landing(props: DefaultProps) {

  return (
    <Box id="landing">
      <Typography>Steak baby yeah steak</Typography>
    </Box>
  )
}